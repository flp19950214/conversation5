<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--上面是头部命名空间-->
<!--mapper开始就是自己定义的了-->
<mapper namespace="com.kjgs.conversation.mysql.mapper.数据Mapper">    <!--对应的mapper类的包路径-->
    <select id = "查询所有数据" resultType="String">
        select JSON_INSERT(数据对象,'$."数据表下标"', 数据表下标) as 数据对象 from 数据表 order by 数据表下标 desc
    </select>

    <select id = "查询上一个输出对象" resultType="String">
        select JSON_INSERT(数据对象,'$."数据表下标"', 数据表下标) as 数据对象
        from 数据表
         where JSON_CONTAINS_PATH(数据对象, 'ALL', '$."输出的句子"')
         order by 数据表下标 desc limit 1
    </select>
    <select id = "查询上一个输入对象" resultType="String">
       SELECT JSON_INSERT(数据对象,'$."数据表下标"', 数据表下标) as 数据对象
        FROM 数据表
         where JSON_CONTAINS_PATH(数据对象, 'ALL', '$."输入的句子"')
        order by 数据表下标 desc limit 1
    </select>
    <select id = "根据键值对查询" resultType="String">
      SELECT 数据对象 FROM 数据表
      WHERE
      <choose>
          <when test="键!=null and 键!='' and 值!=null and 值!=''">
              JSON_EXTRACT(数据对象,  concat("$.",'"',#{键},'"'))=  #{值}
          </when>
          <when test="键!=null and 键!='' and (值==null or 值=='')">
              JSON_EXTRACT(数据对象,  concat("$.",'"',#{键},'"'))
          </when>
          <otherwise>
              1=1
          </otherwise>
      </choose>
      order by 数据表下标 desc
        <choose>
            <when test="个数!=null">
                limit ${个数}
            </when>
            <otherwise>
                limit 10
            </otherwise>
        </choose>
    </select>
    <select id = "查询数据表指向" resultType="String">
        SELECT JSON_INSERT(数据对象,'$."数据表下标"', 数据表下标) as 数据对象
        FROM 数据表
        WHERE JSON_CONTAINS_PATH(数据对象, 'ALL', '$."数据表指向"')
        order by 数据表下标 desc limit 1
    </select>



    <insert id="保存数据表">
        insert into 数据表 (数据对象) values (#{数据对象})
    </insert>

</mapper>
